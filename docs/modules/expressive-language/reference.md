# Expressive Language Reference

Continues from [`README.md`](README.md). This reference is split by topic:

- [`reference-nodes.md`](reference-nodes.md) — node kinds (`agent`, `model`,
  `tool_executor`, `router`, `subgraph`, `subagent`, the reserved `steering`
  block, `repl_agent`, `interrupt`, `join`) and capability references.
- [`reference-graph.md`](reference-graph.md) — binding to Rust, the state
  model, routes, policies (including literal values and list separators),
  comments and strings, safety rules, worked examples, formatting goals,
  the testkit, and implementation milestones (L1-L8).
